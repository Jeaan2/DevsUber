import React from 'react';
import { Container, Header, HeaderTitle } from './styles';
const Page = () => {
    return (
        <Container>
            <Header> 
                <HeaderTitle>
                        DevsUber
                </HeaderTitle>
            </Header>
        </Container>
    );
}

export default Page;